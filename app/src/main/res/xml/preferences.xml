<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:key="pref_use_data"
        android:title="@string/use_data"
        android:summary="@string/use_data_description"
        android:defaultValue="false" />
    <!-- todo ENABLE THIS ONCE I FIGURE OUT HOW TO HANDLE EXCHANGES THAT ARE MISSING PAIRS
    <ListPreference
        android:key="pref_exchange"
        android:title="@string/exchange_select"
        android:summary="@string/exchange_description"
        android:dialogTitle="@string/exchange_select"
        android:entries="@array/exchanges"
        android:entryValues="@array/exchanges"
        android:defaultValue="@string/exchange_default" /> -->
</PreferenceScreen>