<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ColorButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/button_text_selector</item>
        <item name="android:background">@drawable/color_button</item>
    </style>

    <style name="NegativeColorButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/button_text_selector</item>
        <item name="android:background">@drawable/color_button</item>
    </style>

    <style name="SettingsTheme" parent="AppTheme">
        <item name="android:colorBackground">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/colorBackground</item>
        <item name="android:textColorSecondary">@color/colorLightGray</item>

        <item name="android:alertDialogStyle">@style/AlertDialogTheme</item>
        <item name="android:alertDialogTheme">@style/AlertDialogTheme</item>
        <item name="android:dialogTheme">@style/DialogTheme</item>
    </style>

    <style name="DialogTheme" parent="@style/Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/colorBackground</item>
        <item name="android:textColorAlertDialogListItem">@color/colorBackground</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

    <style name="AlertDialogTheme" parent="@style/Theme.AppCompat.Dialog.Alert">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/colorBackground</item>
        <item name="android:textColorAlertDialogListItem">@color/colorBackground</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

</resources>
